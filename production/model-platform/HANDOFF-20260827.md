# 模型平台交接入口（更新至 2026-08-28）

> 文件名保留 `20260827` 是为了与既有 `AGENTS.md` 和历史链接兼容。
> 本文只负责交接导航和继续顺序；当前生产事实以
> `CURRENT-STATE-20260828.md` 为准。

## 1. 必读顺序

1. 仓库根目录 `AGENTS.md`：安全边界、主机角色和发布规则。
2. `production/model-platform/CURRENT-STATE-20260828.md`：当前生产快照、未发布变更、已知异常和下一步。
3. `production/model-platform/TARGET-ARCHITECTURE.md`：唯一 v2 目标架构。
4. `production/model-platform/ROADMAP.md`：当前到目标的实施优先级和责任边界。
5. `production/model-platform/README.md`：目标方案、当前状态和实施记录的文档导航。
6. `production/model-platform/identity-operations-20260825.md`：机器身份、权限、受控凭据路径和轮换记录。
7. 当前任务对应的组件方案和发布记录。

开始前必须执行：

```bash
cd /home/ilya/Desktop/Material
git status --short
git branch --show-current
git log -3 --oneline --decorate
```

## 2. 当前交接结论

- 生产控制面正常：Artifact Keeper、Gitea、Argo CD、Tekton、Crossplane、Backstage 和 KubeRay Ready。
- K12 CPU 数据管线的 GitOps、Dagster/Ray CPU、状态迁移、Backstage 启动和 NodePort 切换已完成。
- 旧 `k12/mineru-dagster` 为 0 副本回滚对象，不得与新 Dagster daemon 长期并行启动。
- Qwen3.8 推理当前为 Stopped/paused；本地 Running 请求尚未发布。
- 模型缓存 Job immutable 错误和通用 ModelDeployment CI 失败尚未解决。
- 训练侧为独立团队正在联调的范围；当前 Argo 应用 OutOfSync，存在状态 schema 和调度/探针问题。
- Material 工作区包含大量已上线但未提交的源码、清单和记录，当前最高优先级是整理可审查提交。

## 3. 继续工作顺序

1. 根据 `CURRENT-STATE-20260828.md` 将已上线变更与本地未发布变更分开。
2. 拆分并审查 Material 提交，不要把 K12、Backstage、推理 Running 请求和训练侧文件混成一个提交。
3. 恢复通用 GitOps CI 绿色，再处理任何推理 Running 发布。
4. 设计并验证模型缓存 Job 版本化。
5. 将训练侧问题交由训练责任人收敛，不在推理/K12 任务中顺手修改。
6. 完成 Artifact Keeper 容量和 `server-00` 维护评审。

## 4. 严格边界

- 生产 Kubernetes 命令只能使用 `sudo k3s kubectl`。
- Argo CD 继续人工 Sync，prune/self-heal 保持关闭，除非有新的明确政策批准。
- 不得把“镜像已发布”、“本地校验通过”或“文件已存在”写成“生产已部署”。
- 不得在未审批时启动 NPU、RayService、模型缓存 Job 或新 TrainingRun。
- 不得打印、提交或复制密码、Token、Docker config、Secret data 和 kubeconfig。
