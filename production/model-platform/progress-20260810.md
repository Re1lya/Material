# 模型平台生产接入进度记录

> 状态日期：2026-08-19
>
> 目标集群：`server-00` 所在生产 K3s；命令必须使用 `sudo k3s kubectl` 或显式
> `/etc/rancher/k3s/k3s.yaml`，不能依赖登录用户的当前 context
>
> 当前阶段：Artifact Keeper、Gitea、Argo CD、Tekton CI、Crossplane Core/API 和
> Backstage 门户基础已完成；Recipe 代码已连接 Gitea/Tekton/手工 Argo 链路，
> 但最新 Recipe 源码尚未进入生产 Backstage 镜像；ora CI 已暂停。
>
> 重要边界：本文只把在生产 K3s 中实际执行并验证成功的事项标记为“已完成”

## 1. 当前结论

生产接入目前已经完成五项独立基础模块和两个最小控制闭环：

1. Artifact Keeper 已在 `artifact-keeper` namespace 中稳定运行，使用
   `/mnt/data` 上的 500GiB 本地持久化容量保存模型制品和 PostgreSQL 数据。
2. Gitea 已在 `gitea` namespace 中稳定运行，使用独立的 120GiB 本地持久化
   容量保存 Git 仓库和 PostgreSQL 数据。
3. Argo CD 已在 `argocd` namespace 中稳定运行，使用内部 Registry 中按摘要固定的
   镜像，自动生成的 `default` AppProject 已锁死。
4. Gitea 到 Argo CD 的首个最小闭环已完成：专用只读机器人、单仓库凭据、最小权限
   AppProject 和手工同步 Application 均通过验收，唯一受管对象是隔离 namespace
   中的一个 ConfigMap；随后完成了受控更新、旧提交回滚和最新提交前滚恢复。
5. Gitea 到 Tekton 的首个最小 CI 闭环已完成：main push 经集群内 webhook、CEL
   过滤和只读精确 commit checkout 后，完成离线 Kustomize 与结构校验；手工和 webhook
   两种 PipelineRun 均 `Succeeded=True`，且没有 Git 写入、Argo 同步、PVC 或 NPU 请求。
6. Crossplane Core 2.3.4、RBAC Manager 和 Function Patch and Transform v0.8.2
   已在生产运行；namespaced `ModelDeployment` XRD 已 `Established=True/Offered=True`。
   runtime-zero Composition 已安装，一个停止的控制面 XR 已 Synced/Ready，只组合
   status ConfigMap、Service 和 replicas=0 Deployment，不产生 Pod、Job、PVC、Ray 或 NPU。
7. 2026-08-12：CI 工具镜像 v0.2.0（git + kubectl + python3 + check-jsonschema，
   amd64）已构建并发布到 8889（digest `sha256:e83607f1...`），同 digest 副本也
   推入 Artifact Keeper OCI 仓库。2026-08-17 在 `server-00` 通过只读 Secret
   完成认证拉取验证后，生产 Pipeline 的三个校验 Step 和 status-finally Step
   已迁移到 `110.120.0.3:30670/container-images` 的同一固定 digest；8889
   引用仍保留为回滚基线。
8. 2026-08-12：GitHub EventListener `model-platform-github` 已上线（Pod 1/1
   Ready、`/live` 200、Service 8080），HMAC Secret `github-webhook-secret` 已
   创建，NetworkPolicy 已覆盖双 listener；原 Gitea listener 未受影响。
9. 2026-08-12：ModelVersion/ModelRuntimeProfile JSON Schema 与校验脚本
   `validate-catalog.py` 已完成并通过本地测试（schema、manifestDigest 重算、
   交叉引用），RuntimeProfile 镜像已改为 digest 固定；catalog 文件已同步进
   gitops 仓库树，尚未推入 Gitea/Argo。
10. 2026-08-12：GitHub webhook 入口链路已打通：cloudflared 2026.7.3 通过
    国内镜像加速器拉取并推入 8889（`sha256:b392761b...`，amd64）；port-forward
    （18080→el-model-platform-github）与 Quick Tunnel 运行中（临时 URL
    `https://complications-magnificent-segments-blvd.trycloudflare.com`）；
    GitHub webhook 配置成功（ping 202/绿色）；PR #6 触发 GitHub-labelled
    PipelineRun（revision=PR head SHA `ab84e54d...`，Succeeded，
    bootstrap_validation=PASS），Gitea 入口未受影响。
11. 2026-08-12：ora-desktop CI 缓存 PV/PVC 已创建：StorageClass
    `ora-desktop-cache-local`、PV `ora-desktop-cache-server-00`（100Gi RWO
    Retain，目录 `/mnt/data/model-platform/ci-cache`）、PVC
    `model-platform-ci/ora-desktop-cache`，全部 Bound；未触碰其他组件的 PV。

12. 2026-08-14：Backstage 已在生产 `backstage` namespace 运行，Deployment 和
    PostgreSQL 均 `1/1 Ready`，健康接口 HTTP 200，镜像 digest 为
    `sha256:fee9830d4ba7f99234033bbfde10c1a5e51edda908c734de70f30015ca92a934`
    (`linux/amd64`，Artifact Keeper `container-images`)；`artifact-keeper-backstage-pull`
    为命名空间内只读拉取 Secret。OIDC 登录已映射至 `User:default/gitadmin`，Catalog、Kubernetes
    只读 RBAC 和受限 Gitea PR action 均通过；PR #1
    `backstage-acceptance-20260814` 已创建且没有产生 XR/工作负载。
13. 2026-08-14：临时 Gitea token `backstage-bootstrap-20260813` 已由用户确认
    撤销；永久 `backstage-secrets/GITEA_REQUEST_TOKEN` 保留。Events backend 已加入
    并随 v0.2.9 部署，Scaffolder 404 warning 已消除。生产 Gitea PR
    Trigger/status writer 已部署；真实 close/reopen webhook 触发
    PipelineRun `model-platform-config-validation-qhz2f`，三项校验通过，
    `tekton/model-platform-policy=success`。
14. 2026-08-13：确认环境存在两个镜像仓库，并把 Artifact Keeper
    `110.120.0.3:30670/container-images` 定为所有新集成平台镜像的统一目的地；
    `110.120.0.3:8889` 仅作为现有 digest 引用的兼容仓库。`server-00` 的 K3s
    containerd 已注册 30670 内网 HTTP endpoint，重启后节点恢复 Ready，Pod 总数
    102→102，且当前没有非 Running/Succeeded Pod。2026-08-14 使用临时 Secret 的
    测试 Pod 已在 `server-00` 从 Artifact Keeper 按预期 digest 拉取并成功执行，
    随后 Pod/Secret 均已删除。其他 worker 尚未配置 30670 HTTP endpoint；正式迁移
    尚未执行，但 Backstage、Crossplane 和 `model-platform-ci` 已分别创建永久、仓库只读的
    `imagePullSecret`（CI Secret 实际名称为 `artifact-keeper-image-pull`）；其余
    worker 仍需逐节点注册并验证后才能消费该 endpoint。

15. 2026-08-14：生产 Gitea PR policy path 已发布：Gitea main 已提交校验
    materials（commit `66f6940`），Gitea PR trigger、exact-head validator 和
    `gitea-ci-status-writer` 已部署。PR #1 分支 head `1295588...` 的 synthetic
    PR event 触发 PipelineRun，bootstrap/catalog/ModelDeployment 三个校验均
    通过，最终 `tekton/model-platform-policy=success`；没有创建 XR/Pod/Job/PVC/NPU。
    Gitea 实际 PR webhook delivery 已通过 close/reopen 事件单独确认，未产生
    XR/Pod/Job/PVC/NPU。

16. 2026-08-14：旧 CPU Kind POC `kind-platform-poc-2` 已按精确集群名清理。
    三个目标节点容器、匿名运行时 volume、旧 kubeconfig context
    `kind-kind-platform-poc-2` 均已移除；共享 `kind` network、其他 Kind 集群、
    Registry 容器和生产 K3s 未修改。空的 `/home/admin/poc` 与
    `/tmp/platform-poc-2.kube` 也已删除。完整证据见
    `poc-cleanup-record-20260814.md`。

17. 2026-08-14 只读复核：生产 K3s 的 10 个节点均 `Ready`；Backstage、Artifact
    Keeper、Gitea、Argo CD、Crossplane 和 Tekton 控制面 Pod 均处于预期运行状态。
    Backstage 健康接口 HTTP 200，Artifact Keeper 两个 PVC、Backstage PostgreSQL
    PVC 均 `Bound`，Argo `model-platform-bootstrap` 为 `Synced/Healthy`，
    runtime-zero XR 为 `Synced/Ready` 且生成 Deployment `replicas=0`。Tekton
    两个 EventListener `Ready=True`；当前 9 个 PipelineRun 为 6 成功、3 历史失败，
    失败集中在旧 status-report TaskRun，最新校验仍成功。本次仅执行读取和文档更新，
    未清理 PipelineRun/TaskRun，未触碰 NPU 或其他运行中工作负载。

18. 2026-08-17：用户已将新的 `ci-images-reader` 只读 token 交互式写入
    `model-platform-ci/artifact-keeper-image-pull`，Secret 类型和字段名已验证，
    token 内容未读取或写入仓库。随后创建的临时拉取 Pod 固定在 `server-00`，仅请求
    `10m CPU/16Mi memory`，未声明任何 Ascend/NPU 资源，按 Artifact Keeper digest
    拉取成功后已删除；测试前后 CI Listener 数量和全局 Pending 数量不变。Tekton
    本地清单已准备切换到同一固定 digest，并为触发式 PipelineRun 添加
    `imagePullSecrets`；手工 Run 使用同一显式模板。生产对象随后已完成
    server-side dry-run 和最小范围 apply。

19. 2026-08-17：Tekton CI 镜像迁移完成并验证。生产
    `Pipeline/validate-model-platform-config` 的三个校验 Step 和 Gitea
    status-finally Step 均使用 Artifact Keeper 固定 digest；Gitea
    `TriggerTemplate` 保留原有 `server-00`/amd64/安全约束并注入
    `artifact-keeper-image-pull`。验证 Run
    `model-platform-config-ak-migration-20260817` 的校验与 status-report 均
    `Succeeded`，日志含 `gitea_commit_status=success`，报告 Pod imageID 与
    Artifact Keeper digest 一致，仅请求 CPU/内存且没有 Ascend/NPU。为释放
    `model-platform-ci` 小额 ResourceQuota，验证完成 Pod 已删除；两个 Listener
    仍 Running、全局 Pending 为 0，未触碰其他服务或 NPU。

20. 2026-08-17：Tekton Operator event-based Pruner 已开启。单例
    `TektonPruner/pruner` 为 `Ready=True`，仅对 `model-platform-ci` 使用
    `ttlSecondsAfterFinished=604800`、成功/失败历史各 10 条；旧式 Pruner
    CronJob 为 0。首次 Operator 渲染曾把新建 Pruner Pod 指向
    `gpu-server-00/gpu-server-05` 且使用 GHCR，已在 Pod Ready 前停止并清理
    仅这批新建的 Pruner 资源；没有删除、缩放或滚动既有 CI、模型或 NPU
    工作负载。controller/webhook 通过 `ghcr.dockerproxy.net` 校验后复制到
    现有 8889 内部仓库并按 digest 固定，生成 InstallerSet 已加
    `server-00` nodeSelector。最终两个 Pruner Pod 均 Running 于 `server-00`，
    `pruner_gpu_pods=0`、全局 Pending=0、旧 CronJob=0。该 8889 镜像是暂时例外，
    待 Artifact Keeper writer token 可用后迁移到 `30670/container-images`。

21. 2026-08-17：完成 FastAPI 部署和 CI/CD 方案的只读生产复核与文档收敛。
    当前没有名称包含 `fastapi` 的 Deployment、StatefulSet、Service、Ingress 或
    HTTPRoute，也没有 FastAPI 专用 namespace。`server-00` 为 AMD64/64 CPU，调度
    requests 为 29.7 CPU（46%）/53132Mi（6%），实时采样为 4393m/76490Mi；
    `/mnt/data` 余约 1.6TiB。现有 `model-platform-ci` 只有 2 CPU/2Gi requests
    配额并绑定暂停中的 100Gi `ora-desktop-cache`，不适合混入 FastAPI 构建。
    方案因此改为独立 `fastapi-ci` 与 `fastapi` namespace、单并发无特权 CI、
    无 host Docker socket 的可信镜像发布、digest-only GitOps PR、单副本有界首发
    和人工 Argo Sync。此次仅查询与修改本仓库文档，没有创建/修改集群对象、读取
    Secret、启动 Pipeline 或影响现有工作负载。

22. 2026-08-18：只读确认生产 K3s 已有 KubeRay Operator v1.6.0，位于
    `ray-mangement`，Deployment `1/1 Ready`；`ray.io/v1` 的 RayCluster、
    RayCronJob、RayJob 和 RayService CRD 均已注册，并存在 Ready RayCluster 和
    历史 RayJob。本次没有执行任何集群写操作。KCC 预训练集成已明确暂缓：先由
    KCC 侧将宿主机脚本、固定路径、日志状态和 kubeconfig 依赖工程化迁入 K3s，
    再由平台新增业务 `PretrainingJob` CRD/Operator 并复用现有 KubeRay；不得部署
    第二套 KubeRay Operator。当前 Backstage KCC 页面保持 release-gate/read-only，
    训练操作继续禁用。

23. 2026-08-18：根据用户要求把 Qwen3.8-27B 接入收敛为一条最小闭环，并将模型
    源改为 ModelScope。新方案为“ModelScope BF16 一次下载/校验 → Artifact Keeper
    源制品 → 隔离 ModelSlim W8A8 量化 → Artifact Keeper 正式制品 → 现有 KubeRay
    RayService → `gpu-server-00` 8 NPU → 最小
    chat 请求”，先执行一次零 NPU ModelScope→Artifact Keeper→ 缓存读回流程和
    一个 stopped XR/Composition 冒烟，再将同一份快照切到单副本 Ray。首版新增
    的只有锁定的 provider-kubernetes、namespace 限定 RBAC 和 Qwen3.8
    Composition；Argo CD 提交 XR，Crossplane 组合缓存/RayService，KubeRay
    运行 Ray。本条仅更新方案文档，尚未从 ModelScope 下载、创建 NPU 工作负载
    或修改生产对象。

24. 2026-08-18：根据“首次实现必须可复用、不能先做一次性 POC”的要求，进一步
    固化 Qwen3.8 方案：冒烟与正式发布共用同一 Kustomize base、ModelVersion/
    RuntimeProfile、ModelScope importer、Artifact Keeper 读回缓存、Crossplane
    Provider/ProviderConfig、namespace 限定 RBAC、Composition、RayService、
    RBAC/NetworkPolicy/Quota 和 Argo Application；仅用 canary overlay 与
    `desiredState` 区分发车状态。新增幂等/断点/原子发布、缓存互斥、digest/架构、
    `gpu-server-00` Artifact Keeper ARM64 拉取验证和“禁止占位符进入正式 PR”等
    Definition of Done。此项仍为待实施工程基线，未创建生产对象。

25. 2026-08-18：完成仓库侧第一阶段实现（仅本地文件）：新增 CPU-only
    `importer/modelscope_import.py`（按 ModelScope immutable revision 下载 BF16 源、逐文件
    SHA256、Artifact Keeper 幂等读回、manifest 最后发布、失败不覆盖），补齐 ModelSlim
    W8A8 独立量化任务的制品/配置契约和 cache
    image Dockerfile 和 `model-cache-fetch.py` 从 Artifact Keeper 读取同目录
    `manifest.json` 的逻辑；新增 provider-kubernetes RuntimeConfig、namespaced
    ProviderConfig、`model-serving` Role/RoleBinding、Qwen3.8 Ray Composition 和
    XRD/Tekton digest/引用门禁。所有新清单仍未被 bootstrap/Kustomize/Argo 引用，
    因而没有创建 Provider、PVC、Job、RayService、Service 或 NPU Pod。

26. 2026-08-18：只读 ModelScope 预检未确认用户给出的
    `Qwen/Qwen3.8-27B` 仓库和 ModelSlim W8A8 适配；公开检索返回的是
    Qwen3.6-27B-FP8。没有静默替换模型，也没有读取 Artifact Keeper Secret 或执行上传。正式 ModelVersion/
    RuntimeProfile/ModelDeployment 只能在确切 modelId、可读 immutable revision、
    publisher credential、Artifact Keeper 读回和镜像/StorageClass 证据齐全后生成。

27. 2026-08-18：Backstage 本地前端已将 KCC 预训练和模型部署明确拆成两个入口：
    `/kcc-pretraining` 展示 ModelScope→CPU importer→Artifact Keeper→ModelVersion
    发布门禁；`/model-recipes` 提供已提交 ModelVersion/RuntimeProfile 的只读快照、
    Ascend A3 hardware/variant、物理卡 `10-15` 和停止态 resolved recipe。部署按钮
    进入受限 Scaffolder，由后端创建 Gitea PR，不再生成浏览器 mock 成功结果；两个页面
    都不访问 Kubernetes/KCC/gateway，不创建 Ray 或 NPU 资源。生产 Backstage 未因
    本次本地源码变更而发布。完成记录见
    `backstage/model-deployment-recipe-mock-record-20260818.md`。
    本次明确未对 `gpu-server-00` 做任何写操作。

28. 2026-08-18：按“先完成 Crossplane 等基础建设、不处理模型文件”的要求，
    新增并实际应用 `crossplane/foundation/kustomization.yaml`：Provider
    ServiceAccount/`model-serving` Role+RoleBinding、无 NPU 的
    DeploymentRuntimeConfig、ModelDeployment XRD 和 Qwen3.8 Composition。
    server-side dry-run 和正常 apply 均通过，XRD `Established=True`，Composition
    已注册；Provider 包、ProviderConfig、XR、PVC、Job、RayService、Service
    仍未创建。ServiceAccount 权限检查确认只允许 `model-serving` 目标资源，
    拒绝其他 namespace、Nodes、Secrets 和 RayCluster；没有模型下载/量化，
    没有对 `gpu-server-00` 产生 NPU 或工作负载变更。证据见
    `crossplane/foundation-deployment-record-20260818.md`。

29. 2026-08-18：恢复执行后新增 Provider 包镜像脚本
    `crossplane/provider-kubernetes/mirror-package.sh`，支持构建主机通过显式
    `TRANSPORT_PACKAGE_REF` 使用 dockerproxy/pull-through registry，校验
    provider-kubernetes v1.0.0 OCI index 与 Linux/AMD64 子摘要，再复制到 Artifact
    Keeper；生产清单只允许内部 HTTPS digest。2026-08-18 已使用一次性、仓库限定的
    `svc-crossplane-provider-mirror` 的一次性 Token 完成镜像并校验 index、Linux/AMD64 与 Linux/ARM64 摘要，
    随后立即撤销 Token；Provider 包仍未安装。Backstage `/kcc-pretraining` 已改为发布门禁只读页，`/model-recipes`
    改为 Gitea ModelVersion/RuntimeProfile 快照和 Scaffolder GitOps 入口；后端 PR
    强制 `desiredState: Stopped`，不再生成浏览器 mock 成功结果。页面测试 6 项、
    TypeScript、Backend build、YAML/shell 校验均通过。只读集群复核确认 ProviderConfig
    API/资源和 `model-serving` PVC/Job/RayService/Service 均不存在，`gpu-server-00`
    仍为 arm64、8 个可分配 Ascend910，未读取 Secret 值或修改运行程序。
    同步更新 `gitops/repository/ci/schema/modeldeployment.schema.json`，使 Tekton
    接受新的 `declarative-stopped` 请求模式和 allow-listed RuntimeProfile；已用
    Scaffolder 生成形状做 JSON Schema 校验。

本地仍有未发布的 ModelDeployment GitOps Application、schema 和目录材料；生产的
Crossplane Function、ConfigMap-only Composition、聚合 RBAC、stopped XR、Tekton
Gitea PR status path 和 Backstage 门户已按上述记录部署或验证。手工 Argo
materialization 仍未完成。

阶段一、二、三的完整发布顺序、门禁、验收和回滚边界统一记录在
`production/model-platform/backstage/release-runbook.md`。这里的“阶段一”是安全
Crossplane 控制面，“阶段二”是 Backstage 建 PR 与 Tekton 校验/状态回写，“阶段三”
是人工批准后由 Argo CD 手工同步一个 stopped XR。根目录 `AGENTS.md` 已把本进度记录、
总体方案、各模块 README 和该 runbook 列入继续工作的强制阅读顺序；新的 Agent 不需要
读取聊天历史，也不得把本地已准备误报成生产已部署。

网络实测（2026-08-12）：server-00 直连 `github.com` 的 git clone 极慢
（TLS 中断、持续 <1 B/s），而 `codeload.github.com` tarball 下载约 147KiB/s、
`api.github.com` 约 7s 返回 200、`registry.npmjs.org` 约 1.1s、
`static.crates.io` 可达。git clone 与依赖下载是否需代理，按实测后再定。

Qwen3.6-27B W8A8 模型已经以 24 个原始文件上传至 Artifact Keeper。所有文件的
大小和 SHA256 均已与源服务器逐一核对，完整下载抽查也通过 SHA256 校验。

生产 ModelVersion、认证运行时和节点缓存程序的声明式材料已经准备完成，但尚未
应用模型缓存 Job，也没有创建新的推理工作负载或申请任何 NPU。现有训练、推理、
Ray、监控和其他 namespace 中的工作负载没有被本阶段修改。

### 2026-08-18 暂停点：Provider 镜像代理与 Backstage 入口调整

本轮在用户要求暂停前已完成以下仓库变更，未执行模型处理或生产运行时变更：

- 新增 `crossplane/provider-kubernetes/mirror-package.sh`。它允许在构建主机上
  通过显式 `TRANSPORT_PACKAGE_REF` 使用 dockerproxy/pull-through registry，校验
  provider-kubernetes v1.0.0 的 OCI index 与 Linux/AMD64 子摘要后，复制到
  Artifact Keeper `110.120.0.3:30670/container-images`；代理地址不会进入生产
  Provider 清单。2026-08-18 已用 `svc-crossplane-provider-mirror` 一次性 Docker `read/write` Service Account 完成
  同等摘要校验，Token 已撤销，Service Account 无 active token。
- `crossplane/versions.lock.yaml` 增加 Artifact Keeper 目标 tag、内部 HTTPS
  package 模板和传输边界；`provider-package.example.yaml` 已填入经校验的内部
  digest，Provider/ProviderConfig 仍未安装。
- Backstage `/kcc-pretraining` 改为只读发布门禁页面，展示
  ModelScope → CPU importer → Artifact Keeper → Gitea ModelVersion →
  Tekton/Argo/Crossplane 链路，不再显示虚构训练节点或 NPU 状态。
- Backstage `/model-recipes` 改为 Gitea ModelVersion/RuntimeProfile 只读快照和
  停止态 resolved recipe；部署按钮进入受限 Scaffolder 模板，由后端创建
  `desiredState: Stopped` 的 Gitea PR，不在浏览器生成 mock 成功结果。
- 模板和后端 action 已将 `mock-fixed-runtime` 改为 `declarative-stopped`，并把
  RuntimeProfile、Tekton、人工合并、Argo CD Sync、Crossplane 的责任边界写入 PR。
- Backstage 页面定向测试（6 tests）、TypeScript `--noEmit`、YAML parse、shell
  syntax 和 `git diff --check` 已通过；仅有既有 Material UI `findDOMNode` 测试警告。

暂停时仍保持：没有下载/量化模型、没有安装 Provider、没有创建 ProviderConfig、
PVC、Job、RayService 或 Service，也没有修改 `gpu-server-00`、NPU 或其他正在运行的
程序。该暂停点是历史状态；继续执行后 Provider/ProviderConfig 已单独完成审计和
安装，模型与运行时仍未执行。

### 2026-08-19 继续点：provider-kubernetes 基础建设

- Provider `provider-kubernetes:v1.0.0` 已从 Artifact Keeper 不可变 digest 安装，
  运行在 AMD64 `server-00`，`Installed=True/Healthy=True`；初次运行镜像因节点无法
  解析 `*.svc.cluster.local` 进入 `ImagePullBackOff`，已在 RuntimeConfig 中显式固定
  到已注册的 `110.120.0.3:30670` runtime image digest 后恢复。
- namespaced `ProviderConfig/model-serving` 已创建，使用 `InjectedIdentity`；生成的
  Provider API RBAC 和 `model-serving` Role 已完成审计。临时 ConfigMap Object 探针
  `Synced=True/Ready=True` 后已删除，未留下 managed Object 或 ConfigMap。
- 当前仍未创建 ModelDeployment XR、PVC、缓存 Job、RayService、Service、模型文件或
  NPU 请求；`gpu-server-00` 和现有工作负载没有变化。

### 2026-08-19 继续点：Qwen3.8 制品与 stopped XR

- 只读检查 `a3-server-00` 确认已有 `/home/models/Qwen3.8-27B-w8a8`（26 个文件、
  `32152070926` bytes）和 `vllm-ascend:qwen3.8-a3` ARM64 镜像归档；A3 上现有
  Docker/vLLM 服务和 NPU 没有被停止、重启或修改。
- 已将已量化的 A3 快照作为 `a3-preloaded` 发布到 Artifact Keeper
  `model-artifacts`，不可变前缀为
  `qwen3.8-27b/w8a8/e823e888ae179eb3be02c1a48899c4f828371376/msmodelslim-w8a8-a3-f2afa9e2`，
  manifest digest 为 `sha256:f2afa9e2f328d9efb78bc88d526413783304c4706a508f0da1db456aeac5c20f`。
  26 个文件和 manifest 共 27/27 逐项校验通过；上传采用 64 MiB 可恢复分块接口，
  没有再次触发大请求内存峰值。
- vLLM-Ascend 镜像已复制到 Artifact Keeper，运行时 manifest 固定为
  `sha256:a27a79c2021cdda071eb207c169a5dd44537d22df11ccd7b62b52de117ceac14`；缓存
  镜像固定为 `sha256:2c38a8bb8be05414a5afebdd88fde3c2032098d0d1edb3f3e005f0adb6d78e13`。
- Gitea `model-platform-config` `main` 已发布 release commit
  `45460d9748e116e0b8c8633c64aa1459e9d8d2d0`，以及补齐 Ray RuntimeProfile
  schema 的 follow-up commit `6fdb3022b791d56cc1cba3a14fce0aca64eec7cf`。Argo CD
  `model-platform-deployment-requests` 通过项目作用域的只读仓库凭据手工同步到
  follow-up commit 并报告 `Synced/Healthy/Succeeded`；Tekton 对该 commit 的
  catalog/ModelDeployment 校验和 Gitea policy status 均为 success。
- Crossplane XRD 结构化 schema 修复后，XR 内部 composition selector 显式固定为
  `modeldeployment-control-plane-v1alpha1`。最终只保留
  `model-serving/qwen38-27b-status`（AwaitingApproval、runtime/cache disabled、
  NPU=0），没有 Qwen3.8 PVC、Job、RayService、Service、Deployment、Pod 或 NPU
  请求。切换期间短暂出现的 runtime-zero Service/Deployment 已由 Crossplane 清理。

完整制品证据见 `qwen38-artifact-transfer-record-20260819.md`；Crossplane/XRD
修复和最终资源边界见 `crossplane/foundation-deployment-record-20260818.md`。

### 2026-08-19：生产监控与 NPU 容量只读盘点

- `monitoring` 命名空间中的 Prometheus、Grafana、Alertmanager、kube-state-metrics、
  Node Exporter 和 OpenTelemetry 组件均为 Running；Prometheus 通过
  `monitoring/npu-exporter-sm` 每 30 秒抓取 NPU exporter。
- `npu-exporter` DaemonSet 为 `9/9/9`，Prometheus 发现的 9 个 target 全部
  `up=1`，覆盖 `a3-server-00` 和 `gpu-server-00/01/02/03/05/06/07/08`。
- 04:43 UTC 快照中，A3 的 16 个 NPU 平均整体利用率为 45.5%、最大值 100、识别到
  8 个进程；`gpu-server-00` 的 8 个 910B3 平均/最大整体利用率均为 0、进程数为 0。
  这只是实时观测，不等价于 Kubernetes 调度保证或自动空闲窗口。
- 当前 K3s 节点清单没有 `a2-server-*`、A2 或 `110.122.*` 节点；`gpu-server-*` 是
  910B3 池。如果 A2 是集群外主机，需要单独部署 exporter 并以 target/remote_write
  接入，现有监控不会自动覆盖它。
- 当前未发现 NPU 专用 `PrometheusRule` 告警；后续可以增加只读 recording rule/
  告警作为发布门禁输入，但不得直接绕过人工审批自动创建 RayService 或申请 NPU。

详细组件、查询、指标字段和快照表见 `monitoring-status-20260819.md`。

## 2. 总体状态矩阵

| 模块或能力                     | 生产 K3s 状态                                                                                                                                  | 当前证据                                                                                                                                                                                                                                                                     | 下一门禁                                                                                                                                                         |
| ------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 生产接入总体方案               | 已完成基线设计                                                                                                                                 | `model-platform-production-integration-plan.md`                                                                                                                                                                                                                              | 随实施结果持续修订                                                                                                                                               |
| Artifact Keeper                | 已部署并验证；server-00 已注册并成功认证拉取 OCI 镜像                                                                                          | Helm、Pod、PVC、健康接口正常；Backstage、Crossplane 与 CI namespace 已分别配置仓库只读 pull Secret；临时 Pod 按 digest 拉取并执行成功                                                                                                                                        | 按需逐节点注册；后续内部 DNS/TLS、备份和声明式 NodePort                                                                                                          |
| Qwen3.6 模型制品               | 已上传并完整校验                                                                                                                               | 24/24 文件的大小和 SHA256 通过                                                                                                                                                                                                                                               | 固化发布工具和只读 Runtime Token                                                                                                                                 |
| Qwen3.8-27B（ModelScope）+ Ray | A3 W8A8 制品已发布；XR 停止态已进入控制面                                                                                                        | Artifact Keeper 前缀、manifest、vLLM/cache 镜像均按摘要固定并完成逐项校验；Gitea `6fdb302`、Argo `Synced/Healthy`、Crossplane ConfigMap-only XR 已验证；无 PVC/Job/RayService/Service/Pod/NPU | 评审并单独提交 `desiredState: Running`、缓存 READY 和 8-NPU 窗口；当前不自动发车 |
| Gitea                          | 已部署并验证                                                                                                                                   | 健康、认证、Git 仓库、重启持久化、监控和禁止 force push 均通过                                                                                                                                                                                                               | 备份、TLS、独立评审身份和仓库 CI                                                                                                                                 |
| 最小 GitOps 闭环               | 已完成并验证                                                                                                                                   | 更新、人工回滚、前滚恢复和 tracking-id 一致                                                                                                                                                                                                                                  | 自动执行 schema/CI，继续保持低权限人工同步                                                                                                                       |
| ModelVersion 目录              | Qwen3.8 文件已写入 Gitea 并接入 CI                                                                                                  | YAML、manifest、整体摘要、JSON Schema、交叉引用和校验脚本均通过；ModelScope revision 与 `a3-preloaded` 来源均留痕                                                                                                                                                                                                                | 保持 manifest/digest 不变；任何重导入必须生成新 immutable prefix                                                                                                                                        |
| ModelRuntimeProfile            | Qwen3.8 stopped profile 已写入 Gitea                                                                                                     | ARM64 vLLM-Ascend digest `sha256:a27a79c2...`、cache digest `sha256:2c38a8bb...`、8-NPU contract 已固定；旧 Qwen3.6 profile 仍独立                                                                                                                                                                              | 仅在人工评审后切换 Running，并再次核对节点和 NPU 窗口                                                                                                                                |
| ModelCache 下载器              | 代码和 Job 已准备，尚未运行                                                                                                                    | 支持续传、逐文件 SHA256 和原子发布                                                                                                                                                                                                                                           | 创建只读 Secret，人工批准节点预取                                                                                                                                |
| NPU 模型缓存                   | 未执行                                                                                                                                         | 没有缓存 Job 或新模型副本                                                                                                                                                                                                                                                    | 确认节点磁盘、网络和现有负载后执行                                                                                                                               |
| Qwen 生产端到端部署            | 未执行                                                                                                                                         | 当前只复用了现有运行方式作为模板                                                                                                                                                                                                                                             | 等 NPU 空闲后进行独立验证                                                                                                                                        |
| Argo CD                        | Qwen3.8 stopped request 已完成手工同步                                                                                                                       | `model-platform-deployment-requests` 使用项目作用域只读凭据，commit `6fdb302` 为 `Synced/Healthy/Succeeded`；未启用自动同步、prune 或 self-heal                                                                                                                                                                                                                    | 保持人工评审和显式 Sync；Recipe 发布只更新 Backstage，不改变 Argo 策略                                                                                                                   |
| Crossplane                     | Core、Function、provider-kubernetes、namespaced ProviderConfig、control-plane Composition 和 Qwen3.8 XRD 已验证                              | Function/Provider Installed+Healthy；XRD 结构化 schema 修复后注册；XR `Synced/Ready`，最终仅 status ConfigMap；无 PVC/Job/Ray/NPU | 先验证缓存 READY，再评审 `desiredState: Running` 和 NPU 窗口 |
| Tekton CI                      | v0.2.0 已发布，双 EventListener、catalog/ModelDeployment 校验、Gitea status path 和 event-based Pruner 已上线；CI 镜像已迁移至 Artifact Keeper | 迁移验证 Run `model-platform-config-ak-migration-20260817` 成功；Qwen3.8 首次 webhook 因 Gitea 缺少 RuntimeProfile schema 失败，补齐 schema 后 Run `model-platform-config-validation-2mqpc` 的校验和 status-report 均 `Succeeded`，Gitea policy 为 `success`；无 NPU/Pending 影响；新的 `tekton/artifact-publish/` 只完成本地设计，未创建 namespace/PVC/Secret | 保持 server-00/amd64 约束；CI 必须继续拒绝旧 schema/占位符和 digest 漂移；artifact-publish 需 HTTPS、staging PVC 与 publisher Secret 后单独审批 |
| FastAPI 服务 CI/CD             | 仅完成只读现状复核和部署方案；生产无 FastAPI namespace、workload、Pipeline 或路由                                                              | `server-00` CPU/内存、`/mnt/data`、现有 CI quota/PVC、Tekton Pruner 与 Argo 手工同步边界已复核；未修改集群                                                                                                                                                                   | 先冻结准确源码仓库、lock/test/health contract，再评审独立 `fastapi-ci`/`fastapi` release；不得复用 model CI quota 或 desktop PVC                                 |
| Backstage                      | 已部署；门户、events backend、受限 Gitea PR action、真实 PR status 和 GitOps/只读发布页面源码已验收                                         | v0.2.11 Artifact Keeper AMD64 digest `sha256:5e779eac...`；只读 pull Secret、健康、OIDC、Catalog、RBAC、20Gi Retain PV、真实 PR #1 policy success；本地源码已连接 Gitea ModelVersion/ModelRuntimeProfile、Kubernetes stopped XR 只读状态和受限 Scaffolder PR，但新源码尚未重新构建/发布镜像 | 按 `tekton-argocd-recipe-release-20260819.md` 发布 v0.2.12 并只滚动 Backstage Deployment；不启用自动 Argo/NPU；KCC 控制包迁入 K3s 后再接真实状态                                                                                                      |
| 生产 monitoring/NPU exporter    | 已部署并可查询 A3/910B3 实时指标                                                                                                             | `monitoring` Prometheus/Grafana/Alertmanager 正常；npu-exporter 9/9/9、9/9 target `up=1`；无 A2 节点或 A2 target；无 NPU 专用 PrometheusRule | 增加只读容量 recording rule/告警和外部 A2 接入（若确有该主机）；空闲判断仍需人工结合调度 requests、磁盘和业务窗口                                                                 |
| KCC 预训练 / Ray 集成          | 暂缓；现有 KubeRay v1.6.0 Ready，平台未新增 CRD/Operator                                                                                       | Ray 四类 CRD 已注册，现有 RayCluster/历史 RayJob 可读；2026-08-18 仅做只读调查                                                                                                                                                                                               | 先迁移 KCC 脚本/状态/kubeconfig 依赖到 K3s，再评审 `PretrainingJob` CRD + 业务 Operator；复用现有 KubeRay                                                        |
| 安全部署申请闭环               | Backstage action 和 PR policy 已生产验证，Argo/Composition materialization 未闭环                                                              | PR #1 已创建，policy success，无 XR/工作负载；Function/Composition/stopped XR 已由最新生产状态记录                                                                                                                                                                           | 人工评审/合并 PR，Argo 手工 Sync 前再做 diff 和零运行时检查                                                                                                      |
| 稳定模型 API 入口              | 未完成                                                                                                                                         | 尚未创建生产推理 Service/Gateway                                                                                                                                                                                                                                             | 端到端验证通过后增加内部 TLS 入口                                                                                                                                |

## 3. 生产环境边界

### 3.1 目标主机和集群

已确认的目标控制面：

```text
host:       server-00
node IP:    110.120.0.3
Kubernetes: K3s v1.34.6+k3s1
access:     sudo k3s kubectl / explicit production kubeconfig
runtime:    containerd 2.2.2-bd1.34
```

`server-00` 同时承载现有平台工作负载。Tekton 验收时 Kubernetes CPU requests
约为节点可分配 CPU 的 77%（49.55 CPU），内存 requests 约为 16%（129676Mi）。
Crossplane 完成时，由于其他工作负载已经发生独立变化，节点观测变为 28.75 CPU
requests（44%）和 51,340Mi 内存 requests（6%）；不能把这一下降归因于 Crossplane。
Crossplane 自身仅新增 200m CPU / 512Mi 内存 requests，初始实际观测约 5m CPU /
176Mi 内存。调度判断仍以 requests 为准，后续组件必须继续逐项观察。

### 3.2 环境区分

必须持续区分以下两套环境：

| 环境                      | 用途                            | 状态含义                         |
| ------------------------- | ------------------------------- | -------------------------------- |
| `server-00` K3s `default` | 当前生产接入目标                | 本文“已完成”默认指此环境         |
| `kind-platform-poc-2`     | 旧 CPU POC（2026-08-14 已清理） | 历史验证环境，不代表生产已经部署 |

旧 Kind POC 曾验证 Artifact Keeper、Tekton、Argo CD、Crossplane、KubeRay、
RayService 和模型切换等链路，现已删除。其历史版本、存储、网络、资源和认证配置
不能直接视为生产现状；清理边界见 `poc-cleanup-record-20260814.md`。

## 4. Artifact Keeper 已完成情况

### 4.1 部署形态

| 项目                                      | 当前值                        |
| ----------------------------------------- | ----------------------------- |
| Namespace                                 | `artifact-keeper`             |
| Helm release                              | `artifact-keeper`，revision 3 |
| Helm chart                                | `artifact-keeper-1.7.5`       |
| Helm 源码快照                             | `artifact-keeper/helm/artifact-keeper/`，来自 server-00 `/home/admin/artifact-keeper-iac`，同步于 2026-08-20 |
| Backend                                   | `1.6.0`，单副本               |
| Web                                       | `1.5.8`，单副本               |
| PostgreSQL                                | `16-alpine`，单实例           |
| OpenSearch                                | 当前生产精简版未启用          |
| Trivy / Scanner Adapter / DependencyTrack | 当前生产精简版未启用          |
| ServiceMonitor                            | 已启用                        |

当前三个 Pod 均位于 `server-00`，状态为 `1/1 Running`，当前重启次数为 0。

### 4.2 持久化

| 用途       | PVC                                        |  容量 | PV                                    | 主机目录                               | 回收策略 |
| ---------- | ------------------------------------------ | ----: | ------------------------------------- | -------------------------------------- | -------- |
| 模型制品   | `artifact-keeper-storage`                  | 480Gi | `artifact-keeper-artifacts-server-00` | `/mnt/data/artifact-keeper/artifacts`  | `Retain` |
| PostgreSQL | `postgres-data-artifact-keeper-postgres-0` |  20Gi | `artifact-keeper-postgres-server-00`  | `/mnt/data/artifact-keeper/postgresql` | `Retain` |

目录权限已经按容器 UID 固定：Backend 使用 UID 1001，PostgreSQL 使用 UID
999。Pod 重建或节点重启后仍使用相同本地目录，删除 Helm release 不会自动删除
底层数据。

需要注意，本地 PV 中的 `480Gi` 和 `20Gi` 是 Kubernetes 调度容量，不是 ext4
硬配额。它们共享 `/dev/md0`，仍需通过监控和生命周期策略避免占满整个文件系统。

### 4.3 网络和健康

集群内 Service：

```text
artifact-keeper-backend.artifact-keeper.svc.cluster.local:8080
artifact-keeper-web.artifact-keeper.svc.cluster.local:3000
artifact-keeper-postgres.artifact-keeper.svc.cluster.local:5432
```

可信内网临时直连入口：

```text
http://110.120.0.3:30670
```

该入口是独立 NodePort Service，当前使用明文 HTTP。它适合首期内网模型上传、
下载和自动化验证，但仍需纳入正式 GitOps 材料并替换为内部 DNS 和 TLS。

已验证：

- `/livez` 返回 HTTP 200。
- `/readyz` 返回 `status=ready`，数据库、迁移和初始设置均完成。
- `/health` 返回数据库和存储健康。
- PostgreSQL `pg_isready` 正常，目标数据库和账号可用。
- Backend、Web 和 PostgreSQL 日志没有当前 ERROR、panic、checksum mismatch 或
  database error。
- Backend 日志级别已从调试级调整为 `RUST_LOG=info`。
- 管理员初始密码已经完成强制修改，凭据保存在 Kubernetes Secret 中，未写入 Git。

### 4.4 Repository 和容量

```text
repository key:       model-artifacts
format:               huggingface
type:                 local
repository quota:     430GiB / 461708984320 bytes
backend upload limit: 100GiB / 107374182400 bytes
```

Artifact Keeper 当前实际目录约使用 34GiB，PostgreSQL 约使用 79MiB。模型上传后
`/mnt/data` 的观测状态为约 7.0TiB 总容量、5.1TiB 已用、1.6TiB 可用、77% 使用率。

### 4.5 Qwen3.6-27B W8A8 上传验证

源模型目录：

```text
/home/admin/models/Qwen3.6-27B-w8a8
```

Artifact Keeper 路径：

```text
model-artifacts/Qwen3.6-27B-w8a8/
```

验收结果：

| 检查项                         | 结果                |
| ------------------------------ | ------------------- |
| 文件数量                       | 24                  |
| 总字节数                       | 36,446,991,473      |
| Safetensors 权重分片           | 9/9，与索引引用一致 |
| 小文件普通 PUT 上传            | 通过                |
| 大文件分片上传和完成接口       | 通过                |
| 24 个服务端元数据大小核对      | 24/24 通过          |
| 24 个服务端 SHA256 核对        | 24/24 通过          |
| 4,291,131,424 字节权重完整下载 | 37 秒               |
| 完整下载 SHA256                | 通过                |

上传最终采用“保留 Hugging Face 原始目录结构、逐文件上传”的方式，没有把整个
模型长期保存为单个 `.tar.gz` 制品。这样运行端可以按文件续传和校验，不需要同时
保存压缩包与解压目录，也避免单个 29GiB 请求触发后端超时。

## 5. Gitea 已完成情况

### 5.1 镜像供应链

部署前完成了镜像架构和摘要检查，并将镜像复制到内部仓库：

| 项目       | 固定版本或摘要                                                                                                  |
| ---------- | --------------------------------------------------------------------------------------------------------------- |
| Helm chart | `gitea-12.6.0`，SHA256 `bcfe40141cacdb93bbc4974cab8ab0cf37b79254f2d171b928386b5cfbc8be62`                       |
| Gitea      | `1.26.1-rootless`，`linux/amd64`，`sha256:d671880e8fd3e337ea7ce3870bfe8fd37709060cd48224e4df2501c141b85443`     |
| PostgreSQL | `17.6.0-debian-12-r4`，`linux/amd64`，`sha256:42ce087c029b3b61cfb10230c571172f41a5bf2d463871a61bde314b59fa197f` |

运行清单通过 digest 引用 `110.120.0.3:8889/platform/` 中的镜像，不依赖运行时
访问公网。

### 5.2 部署形态和资源

| 项目                   | 当前值                               |
| ---------------------- | ------------------------------------ |
| Namespace              | `gitea`                              |
| Helm release           | `gitea`，revision 2，`deployed`      |
| Gitea                  | 一个 Deployment，固定到 `server-00`  |
| PostgreSQL             | 一个 StatefulSet，固定到 `server-00` |
| HTTP                   | `http://110.120.0.3:30081/`          |
| SSH                    | 禁用                                 |
| 公开注册               | 禁用                                 |
| Gitea Actions          | 禁用                                 |
| Valkey / PostgreSQL HA | 禁用                                 |
| ServiceMonitor         | 已启用且被 Prometheus 发现           |

资源边界：

| 组件       | CPU request / limit | 内存 request / limit | NPU request |
| ---------- | ------------------: | -------------------: | ----------: |
| Gitea      |            500m / 2 |            1Gi / 4Gi |           0 |
| PostgreSQL |            250m / 1 |          512Mi / 2Gi |           0 |

空闲观测约为 Gitea 1m CPU / 85Mi 内存、PostgreSQL 11m CPU / 31Mi 内存。
这些是一次观测值，不替代 requests、limits 和长期监控。

### 5.3 持久化

| 用途       | PVC                     |  容量 | PV                           | 主机目录                                    | 回收策略 |
| ---------- | ----------------------- | ----: | ---------------------------- | ------------------------------------------- | -------- |
| Git 数据   | `gitea-shared-storage`  | 100Gi | `gitea-data-server-00`       | `/mnt/data/model-platform/gitea/data`       | `Retain` |
| PostgreSQL | `gitea-postgresql-data` |  20Gi | `gitea-postgresql-server-00` | `/mnt/data/model-platform/gitea/postgresql` | `Retain` |

初始实际使用量约为 Gitea 328KiB、PostgreSQL 69MiB。上述 PV 同样不是 ext4
硬配额。

### 5.4 验收结果

- Helm lint、template、客户端 dry-run 和 Kubernetes API server dry-run 通过。
- Gitea 和 PostgreSQL Pod 均 Ready，当前重启次数为 0。
- `/api/healthz` 通过，包括 `database:ping` 和 `cache:ping`。
- PostgreSQL `pg_isready` 通过。
- 管理员 API 认证通过。
- 已创建私有仓库 `gitadmin/model-platform-config`，默认分支为 `main`。
- 分别重启 Gitea Deployment 和 PostgreSQL StatefulSet 后，仓库仍可访问。
- `/metrics` 返回 HTTP 200，Prometheus target 为 `up`，无 scrape error。
- 当前日志没有 error、fatal 或 panic。

Gitea 和数据库凭据由 `server-00` 生成并直接写入 Kubernetes Secret。Git 中只
保存 Secret 名称和键引用，不保存真实值。

## 6. Argo CD 已完成情况

### 6.1 部署形态

| 项目           | 当前值                                                               |
| -------------- | -------------------------------------------------------------------- |
| Namespace      | `argocd`                                                             |
| Helm release   | `argocd`，revision 1，`deployed`                                     |
| Helm chart     | `argo-cd-10.1.4`                                                     |
| Argo CD        | `v3.4.5`                                                             |
| 运行组件       | Application Controller、Repo Server、API/UI Server、Redis，各 1 副本 |
| ApplicationSet | Deployment 保留，副本数为 0                                          |
| 对外入口       | 仅 ClusterIP                                                         |
| PVC / NPU      | 无 PVC、无 NPU request                                               |

Dex、Notifications、Commit Server、Redis HA 和 Redis Exporter 当前均未启用。四个
运行 Pod 均固定在 `server-00`，状态为 Ready，当前重启次数为 0。

### 6.2 供应链和资源

Argo CD 与 Redis 镜像均先复制到 `110.120.0.3:8889`，并以 `tag@digest` 固定。
稳定运行 requests 合计为 600m CPU / 1Gi 内存，limits 合计为 3 CPU / 3Gi
内存。一次空闲观测约为 14m CPU / 84Mi 内存。

Helm lint、template、K3s API server dry-run、运行时 imageID、健康接口、日志和
Prometheus target 均已验证通过。Argo Repo Server 还通过一次性测试 Pod 验证了
`gitea-http.gitea.svc.cluster.local:3000` 的集群内连通性。

### 6.3 权限边界

Argo CD 自动生成的 `default` AppProject 仍通过
`argocd/default-project-lockdown.yaml` 移除全部仓库、目标集群和资源权限。首个
专用 `model-platform` AppProject 只允许一个 Gitea 仓库、一个 namespace 和核心
ConfigMap。`model-platform-bootstrap` Application 已完成一次固定 Git SHA 的人工
同步，自动同步、prune 和 self-heal 均未启用。

详细供应链、资源、RBAC 和验收记录见
`argocd/deployment-record-20260810.md`。

### 6.4 最小 GitOps 闭环

| 项目                        | 验收值                                     |
| --------------------------- | ------------------------------------------ |
| Gitea repository            | `gitadmin/model-platform-config`，private  |
| Robot                       | `argocd-reader`，restricted、non-admin     |
| Robot repository permission | pull=true、push=false、admin=false         |
| Token scope                 | `read:repository`                          |
| 初始 Gitea / Argo revision  | `233601ac8a4f5f0bdf6f38cf1b167c314d936283` |
| 当前 Gitea / Argo revision  | `58abbd4bc926ffd3d186f75c924d99dafec4df73` |
| AppProject                  | `model-platform`                           |
| Application                 | `model-platform-bootstrap`                 |
| Destination                 | `model-platform-system`                    |
| Managed object              | `ConfigMap/gitops-bootstrap-status`        |
| Result                      | `Synced / Healthy / Succeeded`             |

首次同步前 Application 为 `OutOfSync/Missing` 且 ConfigMap 不存在，证明没有误开
自动同步。管理员随后执行 `prune=false` 的人工同步；同步后 ConfigMap 带有 Argo CD
tracking-id。第二次变更被检测为 `OutOfSync/Healthy`，在人工同步到 generation 2 后，
又固定旧提交回滚到 generation 1，最后固定最新提交恢复到 generation 2。全程没有
启用自动同步、self-heal 或 prune。

仓库已加入 `ci/validate-bootstrap.sh`：它限制该目录只能渲染一个指定名称和
namespace 的 ConfigMap，并执行客户端 Kubernetes dry-run。生产 K3s 上验证结果为
`bootstrap_validation=PASS`。该脚本目前是可供 CI 调用的门禁，但 Gitea Actions 仍
禁用，所以还不是自动强制执行的 CI。

`main` 已启用第一阶段分支保护：普通 push 仍允许，force push 禁止。审批、状态检查
和签名提交尚未强制，因为当前还没有独立评审者和自动 CI 身份。详细证据见
`gitops/deployment-record-20260810.md`。

## 7. Crossplane Core 与 ModelDeployment API 已完成情况

Crossplane 生产 release 为 `crossplane-2.3.4` revision 2，Core 和 RBAC Manager
各一个副本，均固定到 `server-00` 并保持 Ready。revision 2 为 Core 挂载了 Artifact
Keeper 内部 CA；Core 通过
`artifact-keeper-registry.artifact-keeper.svc.cluster.local` 的 HTTPS 路径解析 xpkg。

Function Patch and Transform `v0.8.2` 已复制到 Artifact Keeper
`container-images` 仓库，Linux/AMD64 manifest 固定为
`sha256:070fd3bdb56ec93f825e2f8fcda902bbdaef2e7831e164be5311144867f51dd8`。
Function 和 FunctionRevision 均为 Healthy/Runtime True。函数运行 Pod 从
`110.120.0.3:30670` 拉取同一摘要，使用只读 pull Secret，并显式以 UID/GID 65532
运行。

`modeldeployments.platform.example.com` XRD 现已 Established/Offered，安装了
`modeldeployment-runtime-zero-v1alpha1` Composition。生产中有一个
`infra-learning/qwen36-pd-crossplane-zero`，其 `desiredState=Stopped`，状态为
Synced/Ready。它只组合：

- 一个状态 ConfigMap；
- 一个 ClusterIP Service；
- 一个永久协调为 `replicas: 0` 的 Deployment。

验收查询确认没有对应 Pod、Job、PVC、RayCluster 或 RayService，因此没有发生 NPU
调度或分配。旧的 `ray-vllm-pd-control-pilot-qwen36-27b` 在发布前后都保持 0 副本，
未被采用、修改或删除。完整过程、HTTPS/CA 路径、RBAC 边界和证据见
`crossplane/deployment-record-20260811.md` 与
`crossplane/runtime-zero-deployment-record-20260814.md`。

生产操作还确认了一个重要上下文风险：`admin` 用户的裸 `kubectl`/Helm 不是生产
入口，且可能指向仍存在的其他 Kind 集群；旧 `kind-kind-platform-poc-2` context
已在 2026-08-14 清理。生产 kubectl 必须使用 `sudo k3s kubectl`，生产 Helm 必须
显式传入 `--kubeconfig /etc/rancher/k3s/k3s.yaml`。

## 8. 已准备但尚未部署的材料

当前仓库的 `production/model-platform/` 还包含以下预备材料：

| 文件                                              | 用途                                                                                                      | 当前状态                                                                                                                                                                      |
| ------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `base.yaml`                                       | `model-serving` namespace 和无 API Token 的 ServiceAccount                                                | 未应用                                                                                                                                                                        |
| `catalog/qwen3.6-27b-w8a8-20260806.yaml`          | 第一版 ModelVersion                                                                                       | 目录对象，CRD 尚未安装                                                                                                                                                        |
| `catalog/qwen3.6-27b-w8a8-20260806.manifest.json` | 24 个文件的大小和 SHA256                                                                                  | 已由上传结果生成                                                                                                                                                              |
| `catalog/qwen36-w8a8-ascend-a3-v1.yaml`           | 第一版 Ascend A3 Runtime Profile                                                                          | 未应用                                                                                                                                                                        |
| `cache/model-cache-fetch.py`                      | 续传、校验和原子发布缓存                                                                                  | 已通过 Python 语法检查，未在生产运行                                                                                                                                          |
| `cache/qwen-cache-job.yaml`                       | 在 `a3-server-00` 预取模型                                                                                | 未应用，不申请 NPU                                                                                                                                                            |
| `gitea/`                                          | Gitea namespace、PV/PVC、values 和部署记录                                                                | 已用于部署并完成验收                                                                                                                                                          |
| `argocd/`                                         | Argo CD values、默认项目锁定和部署记录                                                                    | 已用于部署并完成验收                                                                                                                                                          |
| `gitops/`                                         | 隔离 namespace、最小权限项目、Application 和 Gitea 初始目录                                               | 已用于首次人工同步并完成验收                                                                                                                                                  |
| `crossplane/`                                     | Core values、版本锁、XRD、Function、runtime-zero Composition 和记录                                       | 已部署；一个 stopped XR 仅生成 Deployment(0)、Service、ConfigMap                                                                                                              |
| `backstage/`                                      | Portal、受限 Gitea PR 动作、Mock TP/PP/replica/priority form、PostgreSQL/PV/RBAC/NetworkPolicy 与 runbook | v0.2.9 Mock 表单镜像已从 Artifact Keeper 以锁定 digest 滚动更新；`artifact-keeper-backstage-pull` 只读 Secret 已配置；Backstage/PostgreSQL Ready；effective replicas 必须为 0 |
| `gitops/modeldeployments-*`                       | 独立 Argo Project/Application，只允许 namespaced ModelDeployment                                          | 本地准备；生产未应用                                                                                                                                                          |
| `gitops/repository/.../modeldeployments`          | PR 目标目录和严格 schema/引用校验                                                                         | 本地准备；生产 Gitea 尚未同步                                                                                                                                                 |

缓存 Job 计划写入：

```text
/home/admin/model-cache/artifact-keeper
```

它不请求 `huawei.com/Ascend910`，但会在 NPU 节点产生约 34GiB 磁盘写入和持续
网络流量。因此即使不消耗 NPU，也必须在确认节点磁盘和现有任务后由管理员批准，
不能因为它是“无 NPU Job”就自动执行。

缓存 Job 还依赖 `model-serving/artifact-keeper-model-runtime` Secret 中的只读
`token`。该 Secret 只能通过集群外受控流程创建，绝不能写入本仓库。

## 9. 尚未完成和已知风险

### 9.1 单机故障边界

Artifact Keeper、Gitea、Argo CD 及数据库均固定在 `server-00`。有状态数据位于
`/dev/md0` 上的本地目录；Argo CD 自身无 PVC，期望状态保存在 Kubernetes CRD
中。Pod 重启和节点重启能够恢复，但以下故障当前不能自动容忍：

- `server-00` 永久损坏。
- `/dev/md0` 或其 RAID0 成员盘损坏。
- 主机目录被误删或文件系统损坏。

PV 的 `Retain` 只防止 Kubernetes 自动删除目录，不等于备份。

### 9.2 网络和身份

- Artifact Keeper `30670` 和 Gitea `30081` 的节点入口当前仍是可信内网 HTTP。
- Crossplane xpkg 已使用集群内 DNS、TLS 和专用 CA；其他消费者尚未统一迁移到
  HTTPS，平台仍缺少统一身份和入口级限流。
- Runtime Token 尚未按生产流程创建和轮换。
- Artifact Keeper NodePort Service 需要纳入声明式版本控制。
- Argo CD UI 当前仅 ClusterIP，尚未接入内部 DNS、TLS 证书和统一身份。
- Argo CD 已有首个 Gitea 只读机器凭据；Token 轮换和吊销流程尚需固化。

### 9.3 容量和资源

- `/mnt/data` 当前仍有约 1.6TiB 可用，但 RAID0 和共享 ext4 没有项目级硬配额。
- Artifact Keeper 430GiB repository quota 是主要应用级保护。
- CPU requests 已比内存更紧张，后续每部署一个控制面模块都应重新观察节点
  requests、limits 和实际用量。
- OpenSearch、扫描与 SBOM 分析暂未启用，因此当前并不是 Artifact Keeper 的全功能
  安全扫描版本。

### 9.4 平台链路

- Gitea 和 Argo CD 已完成一个 ConfigMap 级最小闭环及更新/回滚验证；Gitea 到
  Tekton 的 main push 校验也已完成。ModelVersion、Runtime Profile 和部署意图的
  完整 schema/引用校验已在本地实现，但尚未发布到生产 Gitea/Tekton。
- Tekton、Crossplane Core、Function 和 runtime-zero Composition 的生产版本、内部
  镜像、资源和权限已经固化；Backstage v0.2.9、Events backend、Mock 调度表单和
  真实 PR status path 已生产验收，后续只剩稳定入口和更深的运行时闭环。
- ModelVersion 和 ModelRuntimeProfile 当前只是 Git 目录对象，相关 CRD 尚未安装。
- KCC 预训练仍依赖宿主机脚本、固定目录、日志状态和 kubeconfig；平台接入已经
  暂缓，待这些能力工程化迁入 K3s 后再增加业务 CRD/Operator。现有 KubeRay
  Operator 必须复用，不部署第二套。
- Qwen3.6 还没有从 Artifact Keeper 缓存到 NPU 节点并用新平台声明启动。
- 没有完成“Artifact Keeper → 缓存 → 推理 → 健康检查 → 最小对话”的生产端到端
  验收。

## 10. 对现有工作负载的影响控制

本阶段遵循以下约束：

- 没有删除、缩容或滚动现有 namespace 中的工作负载。
- 没有创建任何请求 NPU 的新 Pod。
- Gitea 和 PostgreSQL 仅调度到 `server-00`，不调度到 ARM64 NPU 节点。
- Argo CD 运行组件仅调度到 `server-00`，没有向其他 namespace 同步资源。
- Tekton 控制面和 CI Listener 仅调度到 `server-00`；CI Runner 不挂载 Kubernetes
  API Token，所有 Tekton/CI namespace 均无 PVC 和 NPU request。
- Crossplane Core、RBAC Manager 和 Function 仅调度到 `server-00`；没有安装
  Provider/Configuration。唯一 stopped XR 只生成 Deployment(0)、Service 和
  ConfigMap，没有 Pod、PVC、缓存 Job、Ray 对象或 NPU 分配。
- 首个 Argo CD Application 仅人工同步一个隔离 ConfigMap；更新、回滚和恢复均固定
  Git SHA 且使用 `prune=false`，没有 Pod、存储、Secret、自动同步或 self-heal 行为。
- 镜像预拉取和部署均使用内部 Registry，不改变现有 Registry 服务。
- Gitea 使用独立 namespace、Service、PV、PVC 和主机目录。
- 使用 `30081` 作为 Gitea HTTP NodePort；该端口不得再用于旧 POC 的临时模型
  port-forward。
- 模型缓存和端到端 NPU 测试保持未执行状态，等待明确的资源窗口。

## 11. 推荐后续顺序

### 阶段 A：完成 Git 事实来源，不使用 NPU

1. 将本目录提交到 GitHub Material，保留设计、部署清单和验收记录。
2. 在现有 Tekton CI 中补充 schema、摘要和引用一致性校验。
3. 把 ModelVersion、RuntimeProfile 和部署意图作为可审计对象推入 Gitea。
4. 在已有禁止 force push 的基础上增加受控写入与独立评审身份；此阶段不下载模型、
   不部署推理。

### 阶段 B：接入生产控制面，不使用 NPU

1. 保持首个 Application 为人工同步，不启用 prune 或 self-heal。
2. 评审第二个低风险目录和资源白名单，验证更新、回滚和监控。
3. Crossplane Core、Composition Function 和 runtime-zero Composition 已完成；下一步
   将其声明式文件纳入独立 Gitea 目录和手工 Argo CD Application。
4. 增加 ModelVersion、RuntimeProfile schema 与引用校验后，再让 Argo CD 人工同步
   独立控制面目录；仍不启用 prune/self-heal，保持 runtime Deployment 为 0。
5. 将需要节点写盘或 NPU 的对象放在独立目录和人工审批门后。

### 阶段 C：模型缓存验证，不申请 NPU

1. 创建 repository-scoped、read-only Runtime Token 和 Kubernetes Secret。
2. 确认 `a3-server-00` 磁盘空间、目录权限和当前网络负载。
3. 人工批准后运行一次 ModelCache Job。
4. 验证 24 个文件、整体 manifestDigest、原子目录和 `READY` 标志。
5. 重跑 Job 验证缓存命中，不重复下载完整模型。

### 阶段 D：NPU 空闲窗口中的端到端验证

1. 再次确认 Device Plugin、NPU Exporter、CPU、内存、磁盘和卡组状态。
2. 使用已认证的 Qwen3.6 Runtime Profile 创建独立测试部署。
3. 只读挂载经过校验的节点缓存，不直接从 Artifact Keeper 边下载边启动。
4. 验证 `/healthcheck`、`/v1/chat/completions` 和最小真实对话。
5. 验证停止、重新启动、失败回滚和缓存复用。
6. Backstage v0.2.11 只读门户和 Mock 调度/KCC 页面已经部署；后续为其配置稳定 HTTPS
   与长期 OAuth 回调地址，不能把 Quick Tunnel 当作生产入口。
7. ModelDeployment API Offered 且 GitOps 路径稳定后，继续维护“创建 Gitea PR”
   的申请模板；模板不直接写 Kubernetes。
8. Qwen3.8 首次上线按 `qwen38-ray-mvp-plan-20260818.md` 由专用 Argo CD
   Application 管理 `ModelDeployment` XR；Crossplane Provider/Composition 组合
   PVC、零 NPU 缓存 Job、stopped/Running RayService 和策略对象，KubeRay 继续
   管理 Ray 生命周期。先完成 stopped XR/缓存验证，再允许 worker=1 申请 NPU。

### 延期任务：KCC 预训练工程化与平台接入

1. KCC 侧先完成脚本、配置、日志/状态持久化和集群内 RBAC 的迁移验收。
2. 迁移完成后定义业务 `PretrainingJob` API、状态条件和权限边界。
3. 实现只负责业务意图到 RayJob 转换的 Operator，复用现有 KubeRay v1.6.0。
4. 先只读/观察模式验证，再单独审批 Ray 写权限和 NPU 端到端测试。

## 12. 下一阶段开始前需要确认

- 是否接受 Gitea 和 Artifact Keeper 当前单机、无异机备份的风险。
- Gitea `model-platform-config` 仓库后续的审批规则、CI 身份和受控写入权限。
- Artifact Keeper Runtime Token 的创建、保管和轮换责任人。
- `a3-server-00` 模型缓存根目录、容量上限和清理策略。
- 第一版 Runtime Profile 中 6 张 NPU 和物理卡组的最终审批方式。
- 第二个 Argo CD 目录的 namespace 和资源白名单；首个 ConfigMap 闭环不应直接扩权。
- ModelVersion/RuntimeProfile API、缓存控制契约和未来 Ray Composition 的生产顺序、
  资源预算与 NPU 审批边界。
- Backstage 入口、Artifact Keeper OCI 和 Gitea 的内部 DNS、证书签发与 CA
  分发方案；Quick Tunnel 不能作为这些服务的长期地址。
- PostgreSQL 和制品/Git 数据的离机备份目标。

## 13. 相关文档

- 总体生产方案：`../../model-platform-production-integration-plan.md`
- Artifact Keeper 架构基线：`../../artifact-keeper-production-architecture.md`
- 旧 CPU POC 实施记录：`../../model-serving-poc-implementation-log.md`
- 生产材料说明：`README.md`
- Backstage 最小可用接入方案：`backstage/README.md`
- Backstage Artifact Keeper/Tekton 发布 MVP：`backstage/artifact-management-mvp-20260819.md`
- KCC 预训练面板集成方案（方案 A）：`backstage/kcc-pretraining-panel-plan-20260817.md`
- Backstage 模型部署 Recipe Mock 完成记录：`backstage/model-deployment-recipe-mock-record-20260818.md`
- Backstage Recipe 实际链路接入记录：`backstage/model-deployment-recipe-integration-20260819.md`
- Gitea 部署记录：`gitea/deployment-record-20260810.md`
- Argo CD 部署记录：`argocd/deployment-record-20260810.md`
- 最小 GitOps 闭环记录：`gitops/deployment-record-20260810.md`
- Tekton 和最小 CI 闭环记录：`tekton/deployment-record-20260811.md`
- Artifact Keeper 分片发布 Pipeline：`tekton/artifact-publish/README.md`
- Crossplane Core 与 ModelDeployment API 记录：`crossplane/deployment-record-20260811.md`
- Crossplane runtime-zero 发布记录：`crossplane/runtime-zero-deployment-record-20260814.md`
- Qwen3.8-27B ModelScope + Ray 最小闭环方案：`qwen38-ray-mvp-plan-20260818.md`
- 生产 monitoring 与 NPU 容量只读盘点：`monitoring-status-20260819.md`
- 旧 Kind POC 清理记录：`poc-cleanup-record-20260814.md`
- 后续 Agent 强制交接说明：`../../AGENTS.md`
- Qwen3.8 TP2 Ray 参数、部署链路与容量测试方案：`qwen38-ray-tp2-deployment-and-capacity-plan-20260819.md`

后续每完成一个生产阶段，应更新本文的状态矩阵、验证证据、资源观测和风险项，
不得用旧 Kind POC 的结论代替生产验收。

## 30. 2026-08-19：Backstage Artifact Keeper / Tekton 发布 MVP（仅仓库侧）

按“Backstage 统一管理制品仓库和受限 Token，并能查看 Tekton 发布任务”的目标，
完成了本地实现和清单渲染设计，尚未对生产 K3s 执行 apply。新增内容包括：

- Backstage `artifact-management` 后端插件：只允许 `gitadmin`，限制仓库前缀、格式、
  500GiB quota；Token 仅仓库范围的 read 或 read+write，禁止 delete/admin/通配；
  Token 明文一次性返回仍由 HTTPS+显式配置双重门禁保护；
- Backstage `/artifact-management` 页面：创建仓库、申请 Token、提交受控
  `staging://` 发布元数据、查看 PipelineRun 状态；没有浏览器大文件上传和 Kubernetes/Ray/NPU
  写权限；
- 独立 `tekton/artifact-publish/` namespace、Pipeline、EventListener、RBAC、Quota、
  LimitRange 和 NetworkPolicy。Pipeline 依次执行输入校验、Artifact Keeper 分片上传和
  SHA256 元数据复核；TaskRun 固定在 `server-00`/amd64，未声明任何 NPU 资源；
- Backstage 只读 RBAC 仅能读取该 namespace 的 PipelineRun/TaskRun/Pod 日志；EventListener
  是唯一创建 PipelineRun 的入口。

本地验证结果：TypeScript、Backend lint/build、Frontend lint/build、页面 disabled-mode
测试和 15 个 Tekton YAML 文档的 PyYAML/镜像 digest/无加速器静态检查均通过。生产启用前仍须
单独审批并创建 `artifact-publish` namespace-local image-pull/publisher Secret、受控 staging
PVC、稳定 Backstage HTTPS，并用小型非敏感制品做首次链路验收。当前生产 Artifact Keeper、
Gitea、Argo CD、Tekton、Crossplane、Backstage 和 NPU 工作负载均未因本条变更受到影响。

## 31. 2026-08-20：Qwen3.8 Ray 2.48 A3 运行时已发布

- jumper 经现有代理下载并校验 43 个 Linux/ARM64、CPython 3.12 wheel；A3 使用
  `--network=none` 离线构建，未访问公网。
- 固定 Python 3.12.13、Ray 2.48.0、PyArrow 20.0.0、vLLM 0.23.0；未改变厂商
  CANN/torch/torch-npu/vLLM-Ascend 栈。
- build gate、只读 driver runtime import gate 和无 driver 的 Ray head import gate
  均通过；派生镜像相对基础镜像没有新增 `pip check` 冲突。
- ARM64 镜像已发布为
  `110.120.0.3:30670/container-images/qwen38-ray-runtime@sha256:16995677e10be892e92d164c7a6c8902f37ccb9c2c2d0d79180664006b55d0fb`；
  一次性 repository-bound publisher token 已撤销，临时 tar/明文 token 已清理。
- TP2 Profile 已锁定新 digest、Ray 2.48、`a3-server-00` 和 `Ascend910`；Composition
  同时约束 Ray head 到目标 ARM64 节点，并通过 `VLLM_PLUGINS=""` 保持 head
  CPU-only。当前 worker 仍为 0，未创建 Pod/RayService/NPU 请求。
- 下一步只有在 A3 Kubernetes allocations 与 `npu-smi` 双重检查确认至少两个
  chip 空闲后，才补齐 registry/cache 前置条件并同步运行态 XR；全部繁忙立即停止。

详细证据：`qwen38-ray-runtime-release-20260820.md`。

## 32. 2026-08-20：Qwen3.8 A3 TP2 调度验证已停止并释放 NPU

- Gitea 的三次运行态修订 `30f60ea`、`3d128eb`、`1adf9fe` 分别完成 Ray 2.48
  resource bundle 兼容、物理 8/9 静态分配和 worker CPU 48 核余量，均通过 Tekton；
  Argo CD 曾人工同步完整 commit `1adf9fecb4a66e4179400b2cd6399c1691ad6c30`。
- 临时绕过全局 Volcano 后，Crossplane/Ray/Kubernetes/device-plugin 已成功把一个
  TP2 worker 绑定到 A3 并分配物理 8、9，device-plugin 注解修正成功；该临时绕过
  已回滚，KubeRay Operator 恢复 Volcano 参数和 `1/1 Ready`。
- 当前阻塞点已推进到容器 sandbox：A3 的 K3s containerd 未注册
  `RuntimeClass/ascend` 对应 handler。模型进程未启动，因此健康检查和容量测试未执行。
- 验证结束后 RayService、RayCluster、PodGroup 和本次 Ray Pod 已清理，A3 的
  Kubernetes Ascend910 分配为 0，物理 8、9 无进程；物理 0、1 原有任务未触碰。
  Gitea commit `0735b3b` 已将 XR 请求改为 Stopped/worker=0，并通过 Tekton 后由
  Argo CD 人工同步为 `Synced/Healthy`。为避免无 suspend 字段的 RayService 自动重建
  head，XR 当前显式 paused，RayService provider Object 已删除；缓存 PVC/Job 和制品
  数据保留，供修复后复用。

完整执行证据、错误边界和恢复步骤见
`qwen38-ray-tp2-execution-20260819.md`。

## 33. 2026-08-21：A3 Ascend runtime 阻塞已通过探针验证

- 使用 `default-scheduler`、`RuntimeClass/ascend` 和一个 Ascend910 的直接 Pod
  探针绕过 Volcano；探针固定到未被现有 K8s 工作负载使用的物理 7，不触碰既有
  `ds` Pod 占用的物理 8–15。
- 探针在 `a3-server-00` 完成 sandbox 和容器创建，以退出码 0 结束；设备分配及
  `AscendReal` annotation 都为 `Ascend910-7`。有效 CRI 配置已注册 `ascend` handler，
  指向 `ascend-docker-runtime`，原 `no runtime for "ascend" is configured` 已解决。
- 探针已经删除，A3 的 Kubernetes NPU 分配恢复为既有 8 个，物理 0–7 无运行进程。
  本次没有创建 RayService/PodGroup，没有改变 Volcano；Volcano 调度兼容仍需单独诊断。
- control plane 读取 A3 kubelet `10250` 容器日志时出现 TLS handshake timeout；它没有
  影响 Pod 创建、退出码、设备 annotation 和 CRI runtime 验收，但需作为独立可观测性
  问题后续检查。

详细记录见 `qwen38-ray-tp2-execution-20260819.md`。
