# Artifact Keeper 生产部署与 Dev Portal 集成总体方案

## 1. 执行摘要

Artifact Keeper 将作为 Dev Portal 平台中的独立制品能力，最终部署在生产 Kubernetes 的专用 namespace 中。它可以先独立上线并完成验证，之后再逐步接入 Backstage、模型训练发布流程和模型服务，无需等待整个平台一次性完成。

本方案取代最初“在 `server-00` 宿主机使用 Docker Compose 独立部署”的设想。生产架构不绑定具体服务器、节点、镜像、端口、目录、数据库版本或存储产品；这些内容必须在产品选型和生产环境调查后确定。

核心原则如下：

- Artifact Keeper 后端采用多副本，单个 Kubernetes 节点故障时下载能力继续可用。
- 元数据由独立的高可用数据库服务保存，大型模型文件不进入数据库。
- 模型制品使用跨节点可访问的持久化存储，容器可写层和单节点本地盘不作为生产主存储。
- Artifact Keeper 作为平台离线交付包中的独立 Helm release，版本固定、独立升级和回滚。
- Backstage 是用户入口，通过后端插件调用 Artifact Keeper；模型服务下载不经过 Backstage。
- 同集群模型服务通过 Kubernetes Service 下载，跨集群消费者通过内部 Gateway 下载。
- 一个 revision 表示一次完整、不可变的模型发布，checksum 贯穿发布和下载验证。
- 生产数据完整备份，目标 RPO 不超过约 1 小时；严重灾难下，4 小时内恢复只读下载能力。
- 当前模型大小、并发量、平台空间预算和 Artifact Keeper 正式版本均未知，资源数值必须通过调查和压测定型。

## 2. 推荐架构图

```text
                         内部用户 / CI
                               |
                               | HTTPS
                               v
                    +---------------------+
                    |   Platform Gateway  |
                    +----------+----------+
                               |
                     +---------+---------+
                     |                   |
                     v                   v
             +---------------+   +------------------+
             |   Backstage   |   | Artifact Keeper  |<----- 跨集群模型 Fetcher
             | Frontend      |   | Gateway 入口     |       Token + HTTPS
             | Backend Plugin|   +---------+--------+
             +-------+-------+             |
                     |                     |
                     | 内部 Service         |
                     +----------+----------+
                                v
  同集群模型 Fetcher ------> +----------------------------------+
  Token + Service DNS        | Artifact Keeper 专用 namespace   |
                             |                                  |
  模型发布流程 ------------> |  Backend A / B / ...             |
                             |  可选 Admin Web UI               |
                             +----------+-------------+---------+
                                        |             |
                              元数据     |             | 模型文件
                                        v             v
                               +-------------+  +------------------+
                               | 高可用数据库 |  | 跨节点制品存储   |
                               +------+------+  +---------+--------+
                                      |                   |
                                      +---------+---------+
                                                |
                                                v
                                      +-------------------+
                                      | 独立备份存储系统  |
                                      | 加密 / 独立权限   |
                                      +-------------------+

               日志、指标、告警、审计由平台可观测体系统一收集
```

Artifact Keeper 在逻辑上管理模型制品，但内部仍分为后端、数据库和制品存储。用户和模型服务只调用 API，不直接访问数据库、PVC 或底层存储目录。

## 3. 组件与职责边界

| 组件 | 负责 | 不负责 |
|---|---|---|
| Artifact Keeper 后端 | repository、revision、文件元数据、权限、Token、上传下载、checksum 记录、审计和生命周期 | 模型训练、模型推理、保存容器镜像 |
| 可选 Web UI | 管理员诊断和基础管理 | Dev Portal 的主要用户体验 |
| 元数据数据库 | repository、revision、用户权限、审计索引、文件定位等结构化数据 | 保存大型模型文件、保存容器镜像 |
| 制品存储 | 权重、配置、词表和模型包等大文件 | 用户认证和业务权限判断 |
| Gateway | 跨集群或集群外入口、HTTPS、统一认证、路由、限流和入口审计 | 保存制品、替代 Artifact Keeper 的资源授权 |
| Backstage 前端插件 | 页面、表单、制品选择和状态展示 | 持有 Artifact Keeper 管理凭据、直接访问数据库 |
| Backstage 后端插件 | 验证 Backstage 身份、适配 Artifact Keeper API、隐藏服务凭据 | 传输模型服务运行时的大文件下载流量 |
| 训练系统 | 训练、产生候选模型和评估结果 | 自动把所有 checkpoint 当作正式模型发布 |
| 模型发布步骤 | 打包候选模型、计算 checksum、上传并形成正式 revision | 训练模型、永久删除正式制品 |
| Fetcher/init container | 使用只读 Token 下载、校验 checksum、解压并准备模型目录 | 发布、修改或删除制品 |
| 模型服务 | 加载模型并提供推理 | 管理 Artifact Keeper 存储和数据库 |
| Helm | 部署组件、引用镜像和 Secret、声明资源需求 | 存储、打包或直接下载大型模型文件 |
| GitOps | 固定期望版本和非敏感环境声明 | 保存 Token、模型文件或数据库内容 |
| Crossplane（可选） | 申请数据库、存储、DNS 等基础设施能力 | 管理模型文件内容或下载模型 |

容器镜像与模型制品必须分开：训练程序和推理程序镜像进入内部镜像仓库；模型权重、配置和词表进入 Artifact Keeper。

## 4. 部署形态比较

| 形态 | 优点 | 局限 | 本方案定位 |
|---|---|---|---|
| Kubernetes | 多副本、调度、健康检查、滚动升级、平台 Gateway 与监控集成 | 依赖成熟的存储、数据库和集群运维能力 | **生产推荐** |
| Docker Compose | 简单、适合单机验证、组件关系直观 | 单机故障域明显，生产高可用和平台集成能力有限 | 仅用于开发或短期验证 |
| systemd | 宿主机生命周期清晰、运行开销低 | 依赖和升级编排复杂，多副本与跨节点能力需自行建设 | 不作为目标形态 |
| 直接运行二进制 | 调试简单、依赖层少 | 隔离、升级、恢复和依赖管理成本最高 | 仅用于产品排障 |

平台采用“一个离线交付包、多个 Helm release”：交付包固定经过验证的组件版本组合，Artifact Keeper 保留独立 release 历史。未发生兼容性或安全需求时保持已验证版本；需要升级时只升级相关组件，并形成新的平台版本基线。

## 5. 资源规划框架

### 5.1 需要先收集的工作负载指标

- 单个模型包的平均、P95 和最大压缩大小，以及解压后大小。
- 一次滚动发布、扩容或节点故障时的峰值并发下载 Pod 数。
- 上传频率、并发上传量、每月新增 revision 数量和旧版本保留时间。
- 期望模型启动时间、下载完成时间和恢复窗口。
- 数据库记录增长、审计日志增长和查询峰值。
- 生产集群可提供给整个平台及 Artifact Keeper 的空间、CPU、内存和带宽预算。

### 5.2 估算方法

| 资源 | 主要估算依据 | 必须实测的指标 |
|---|---|---|
| 后端内存 | 应用基线、请求缓冲、并发连接和 checksum 缓冲；必须支持流式传输 | 单副本空载内存、每个并发流的增量、GC/内存峰值 |
| 数据库内存 | 元数据规模、连接数、索引热集和备份方式 | 缓存命中率、慢查询、连接数、复制延迟 |
| CPU | API 处理、TLS、checksum、压缩/解压和审计 | 每 GB checksum CPU 时间、峰值利用率、请求延迟 |
| 在线磁盘 | 正式制品、增长余量、临时上传、数据库、日志及操作余量 | 月增长率、最大临时上传量、碎片和存储冗余开销 |
| 磁盘吞吐/IOPS | 模型文件以大块顺序读写为主，数据库以小块随机 I/O 为主 | MB/s、读写延迟、并发下载时数据库延迟 |
| 网络 | `模型大小 × 并发数 ÷ 目标下载时间`，再考虑协议和峰值余量 | 实际吞吐、丢包、跨集群延迟、Gateway 长连接稳定性 |
| 日志空间 | 请求量、审计事件量、保留期和采集延迟 | 每日增长、峰值积压和敏感字段检查 |
| 备份空间 | 全量基线、增量变化、保留周期、压缩率和恢复副本 | 每次备份增长、恢复吞吐和校验时间 |

### 5.3 分级规划方式

| 阶段 | 资源策略 | 验收重点 |
|---|---|---|
| 功能验证 | 只验证 API、持久化、认证和恢复流程，不据此推导生产容量 | 正确性和可恢复性 |
| 初始生产 | 多副本、单节点故障容忍；按真实首批模型和滚动发布并发设置资源 | 下载成功率、启动时间、容量告警 |
| 扩大后 | 根据下载吞吐、存储增长和数据库指标水平扩容，必要时评估缓存或数据面优化 | 峰值压力下下载优先、恢复窗口仍达标 |

当前不应给出固定资源数值。大文件大小和并发量均未知，因此正式配置必须经过代表性模型的上传、并发下载、节点故障和恢复压测。

容量分配按以下顺序确定：生产基础设施总容量 → 平台预算 → Artifact Keeper 总预算 → 正式制品、临时上传、数据库、日志和操作余量。备份位于独立存储系统时，应单独核算。

## 6. 网络与安全框架

### 6.1 网络路径

- 同集群模型 Fetcher 使用稳定的 Kubernetes Service DNS，不依赖 Pod IP。
- Backstage 后端插件在同集群时调用 Artifact Keeper 内部 Service，不必绕行外部 Gateway。
- 跨集群模型服务、CI 和管理员通过稳定内部 DNS 与高可用 Gateway 访问。
- Artifact Keeper、数据库和制品存储仅在内部网络提供服务，不直接暴露公网。
- 端口、域名和健康检查路径以目标版本和生产网络规划为准，不在方案中写死。
- Gateway 使用 HTTPS；内部 CA 需要被 Backstage、CI 和模型 Fetcher 信任，并具备证书轮换流程。
- 防火墙只开放明确来源和必要端口；数据库和存储不面向普通消费者开放。
- 受限网络环境需要规划 HTTP 代理和 `NO_PROXY`，确保集群 Service、内部域名、数据库、存储和镜像仓库不错误绕行外部代理。
- 健康检查应区分进程存活与服务就绪，数据库或存储不可用时不应继续接收业务下载。

### 6.2 认证与授权

- Gateway 负责集群外入口认证、TLS、限流和入口审计。
- Artifact Keeper 对来自 Gateway 和内部 Service 的请求都执行最终资源授权。
- 管理、发布和只读下载使用不同身份；模型 Fetcher 只拥有下载权限。
- 首期不引入复杂团队多租户，仅按模型 repository 管理，并保留未来扩展权限的能力。
- Token 限定用途和权限范围，支持轮换；轮换时允许新旧凭据短暂重叠。
- Helm 只引用 Kubernetes Secret 名称，真实 Secret 由受控流程预置或轮换。
- Secret 不进入 Git、Chart、镜像、普通 values 文件或日志；Kubernetes 侧配合 RBAC、审计与 etcd 静态加密。
- HTTPS 保护传输，checksum 保护内容完整性，两者不能互相替代。
- 已发布 revision 不允许覆盖；发生安全事件时可立即撤销访问，保留审计记录。
- 备份加密并使用独立凭据，Artifact Keeper 运行账号不能删除历史备份。

## 7. 数据持久化框架

| 数据类型 | 是否必须持久化 | 建议边界 |
|---|---|---|
| 数据库元数据 | 是 | 独立高可用数据库服务、专属数据库和账号 |
| 正式模型制品 | 是 | 跨节点可访问的对象存储或共享持久化存储 |
| 应用配置 | 是 | 非敏感声明进入 GitOps；环境差异参数化 |
| Secret | 是 | Kubernetes Secret 或集中式 Secret 系统；与 Chart 内容分离 |
| 审计日志 | 是 | 集中日志系统，按合规要求保留 |
| 普通运行日志 | 需要受控保留 | 集中采集并设置容量和保留上限 |
| 临时上传文件 | 条件持久化 | 与正式区隔离、容量受限、失败后自动过期 |
| 备份 | 是 | 独立存储系统、独立权限、加密和完整性校验 |

容器可写层随 Pod 重建、升级或调度而变化，既不稳定也难以单独备份和扩容，因此不能保存数据库、正式制品或需要续传的临时上传。

正式上传流程为：上传到临时区 → 校验大小和 checksum → 完整后发布 → 对消费者可见。未完成文件不能被下载，并按较短规则自动清理。

制品生命周期采用两阶段删除：先标记待删除或隔离，在确认没有正式引用并经过等待期后物理删除；revision 标识保留墓碑记录且不得复用。恶意或高风险制品可以立即禁止访问。

## 8. 与 Backstage、训练和模型服务的集成框架

### 8.1 Backstage 集成

```text
Backstage 前端插件
  -> Backstage 后端插件
    -> Artifact Keeper 内部 API
```

- 前端负责模型列表、revision 选择、状态展示和部署表单。
- 后端插件识别 Backstage 用户、检查权限、适配 Artifact Keeper API，并保护服务凭据。
- 前端浏览器不直接持有 Artifact Keeper 管理 Token，也不直接访问数据库或制品存储。
- 第一阶段优先实现查询、选择 revision 和生成部署引用；上传、删除和权限管理后续逐步开放。
- Artifact Keeper 原生 UI 仅作为可选管理员工具，Dev Portal 是普通用户的主要入口。

### 8.2 训练与发布

```text
训练任务
  -> 产生候选模型
  -> 评估
  -> 独立发布步骤打包并计算 checksum
  -> 上传 Artifact Keeper
  -> 形成正式 revision
```

训练任务不直接拥有生产删除权限，也不把所有中间 checkpoint 当作正式制品。一次 revision 表示一套完整、可部署的模型内容；权重、配置和词表不能随意跨 revision 拼接。

### 8.3 模型服务下载

```text
模型服务创建
  -> Fetcher/init container 启动
  -> 使用 Kubernetes Secret 中的只读 Token
  -> 按 repository、revision 和 artifactFile 下载
  -> 校验 artifactChecksum
  -> 解压到模型目录
  -> 模型服务加载
```

- `artifactKeeperBaseURL` 是环境级参数：同集群使用 Service URL，跨集群使用 Gateway URL。
- repository、revision、artifactFile 和 checksum 由 Dev Portal/部署控制面传递。
- Helm、Crossplane 和 GitOps 只传递引用，不保存、打包或下载大型模型文件。
- checksum 由发布步骤生成，Artifact Keeper 验证并记录，Dev Portal 原样传递，Fetcher 最终校验。
- 校验失败时不得解压或启动模型服务。
- 当前是从零建设，不存在旧 endpoint 迁移；仍应从测试制品和第一个非关键模型逐步完成上线验证。
- 首期由 Artifact Keeper 鉴权后直接返回文件流；只有实际压测证明存在瓶颈且产品正式支持时，才评估签名地址或缓存优化。

## 9. 备份、恢复和升级框架

### 9.1 备份与恢复

- Artifact Keeper 是正式发布模型的权威制品库，不能假定训练环境永远可以重新生成模型。
- 数据库、正式制品、配置、Secret 恢复材料和审计数据需要纳入恢复范围。
- 数据库与制品备份必须形成一致恢复点，避免元数据指向不存在的文件。
- 备份至少保存于与生产主存储不同的存储系统，并使用独立权限；是否增加异地副本由生产灾备等级决定。
- 目标 RPO 不超过约 1 小时；关键发布完成后可以增加额外保护点。
- 严重灾难下，目标是在 4 小时内优先恢复只读下载，其次恢复上传、管理 UI 和辅助查询。
- 定期在隔离环境执行恢复演练，验证文件、checksum、Token/权限和实际下载，而不是只检查备份任务状态。
- 高可用解决组件或节点故障，备份解决误删、数据损坏、存储系统和灾难故障，两者不能互相替代。

### 9.2 升级与回滚

- 平台基线固定 Artifact Keeper、数据库客户端和相关依赖的明确版本，不使用浮动标签。
- 非必要不升级；安全、兼容性或功能需要出现时，先验证候选版本再形成新基线。
- 应用升级可以逐步扩大后端副本范围，优先验证上传、下载、权限和 checksum。
- 数据库迁移必须单独门禁；回滚应用镜像不代表数据库结构可以自动回滚。
- 升级前创建可验证恢复点，确认 API、数据库结构、Token 和制品格式兼容。
- 破坏性迁移需要明确维护窗口、恢复方案和回退点，不与存储格式迁移同时进行。
- 未涉及的其他平台模块保持原版本，不要求整个平台同步升级。

### 9.3 日常运维

- 下载优先于上传、备份和后台任务；资源紧张时可以暂停新上传，但尽量保持正式制品下载。
- 监控请求成功率、延迟、吞吐、并发、错误码、数据库连接、存储容量、临时空间和备份状态。
- 审计登录、Token、权限、上传、发布、撤销、删除和管理员操作，日志不得记录完整 Token。
- 容量接近阈值时先告警、扩容、归档或执行审批清理，不自动删除正式制品。
- 首期不强制建设下载缓存；根据实际启动时间、重复下载量和带宽压力决定是否增加按 checksum 管理的缓存。

## 10. 分阶段实施路线

| 阶段 | 目标 | 输入 | 输出 | 验收条件 | 风险点 |
|---|---|---|---|---|---|
| 1. 环境调查 | 明确生产基础能力 | 集群、Gateway、数据库、存储、DNS、CA、备份和资源预算 | 环境能力清单 | 关键依赖、负责人和限制均可追踪 | 把 POC 单节点能力误当生产能力 |
| 2. 产品与架构确认 | 选择满足契约的 Artifact Keeper 版本 | API 契约、候选版本文档和许可证 | 满足/适配/缺失矩阵 | 多副本、存储、认证、API、迁移和审计均已验证 | 先选版本后被迫修改平台契约 |
| 3. 最小独立部署 | 在专用 namespace 建立独立 release | 已确认镜像、数据库和存储 | 可运行的 Artifact Keeper | Pod 重建不丢数据，健康检查有效 | 持久化或依赖初始化错误 |
| 4. 上传下载验证 | 验证大文件主链路 | 测试制品、发布与下载身份 | 上传、查询、下载记录 | 文件内容和 checksum 一致 | 缓冲、超时、临时空间不足 |
| 5. 认证与不可变性验证 | 建立读写边界 | Token、repository 和生命周期规则 | 权限与审计结果 | 只读身份不能写删，revision 不能覆盖 | 内部 Service 绕过授权 |
| 6. 高可用与容量验证 | 验证单节点故障容忍和资源模型 | 代表性大文件和并发场景 | 压测与故障报告 | 节点故障后下载继续，下载优先策略有效 | 存储或数据库仍有单点 |
| 7. 备份恢复验证 | 达到 RPO/RTO 目标 | 一致性备份和隔离恢复环境 | 恢复演练报告 | 4 小时内恢复下载，checksum 和权限正确 | 只有备份、没有可用恢复流程 |
| 8. Backstage 接入 | 建立 Dev Portal 管理入口 | 稳定 API 和 Backstage 插件边界 | 查询与 revision 选择能力 | 前端不持有管理 Token，可生成正确部署引用 | 前端直接耦合产品内部 API |
| 9. 训练与模型服务接入 | 跑通发布和消费闭环 | 候选模型、发布步骤和 Fetcher | 第一个正式模型服务 | 发布、下载、校验、解压和加载全部成功 | 训练身份权限过大或校验被跳过 |
| 10. 生产基线与运维交付 | 固化可重复交付和生命周期 | 全部验证报告和版本清单 | 离线包、版本基线、运维与灾备文档 | 无公网依赖，可独立升级、备份和恢复 | 文档与实际版本漂移 |

## 11. 主要风险与待确认问题

| 风险或未知项 | 影响 | 处理方向 |
|---|---|---|
| Artifact Keeper 正式版本未选定 | 数据库、存储、认证和 API 能力无法确认 | 先定平台契约，再做候选版本能力验证 |
| 最大模型和峰值并发未知 | 无法确定内存、带宽、超时和临时空间 | 使用代表性模型进行大文件并发压测 |
| 平台空间预算未知 | 无法确定配额、保留期和备份容量 | 先确定平台总预算，再分配 Artifact Keeper 空间 |
| 生产存储能力未知 | 可能无法容忍节点故障 | 验证对象存储或共享持久化能力及产品支持情况 |
| 高可用数据库能力未知 | 元数据可能成为单点 | 优先使用成熟的平台数据库服务 |
| Gateway 与统一认证仍在开发 | 外部入口和用户身份传递方式未定 | Artifact Keeper API 先独立，集成方式以后置适配层实现 |
| 内部 CA、DNS 和跨集群网络未确认 | Fetcher 可能无法建立可信连接 | 在网络阶段验证 DNS、证书链、防火墙和代理 |
| 大文件全部经 Gateway | 可能出现带宽、缓冲和超时瓶颈 | 同集群走 Service；跨集群压测后再考虑优化 |
| 数据库迁移不可逆 | 应用回滚可能失败 | 升级前恢复点、兼容性验证和独立迁移门禁 |
| 误删或 Token 泄露 | 制品丢失或越权访问 | 软删除、独立备份、最小权限、轮换和审计 |

## 12. 下一阶段需要收集的信息

1. Artifact Keeper 候选产品来源、正式版本、维护状态、许可证和离线获取方式。
2. 候选版本支持的数据库、对象存储/共享文件存储、多副本和数据库迁移方式。
3. 上传、下载、repository、revision、checksum、Token、审计和健康检查的正式 API。
4. 生产 Kubernetes 节点与故障域、StorageClass、Gateway、内部 DNS、CA 和网络策略能力。
5. 可用高可用数据库服务、备份平台以及独立备份存储位置。
6. 平台总 CPU、内存、在线存储、备份存储和网络带宽预算。
7. 代表性模型的压缩大小、解压大小、上传频率、版本增长率和保留要求。
8. 最大一次滚动发布、扩容或节点故障时的并发下载 Pod 数。
9. Backstage 当前前后端架构、认证方式，以及首期需要展示和操作的 Artifact Keeper 功能。
10. 模型发布包规范、训练评估门禁和 Fetcher 支持的下载、重试、校验与解压能力。
11. 内部镜像仓库的生产地址、TLS、认证、高可用和镜像扫描策略；`110.120.0.3:8889` 仅作为待确认候选。
12. 日志、指标、审计、Token、制品和备份的保留周期或合规要求。

在这些信息收集完成前，不应生成生产安装配置或锁定资源数值。下一阶段的正确产物是环境调查表、产品能力矩阵、API 契约和容量压测计划，而不是直接进入安装。
